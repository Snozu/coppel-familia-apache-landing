---
// Call-to-Action section - restored to original purpose
---

<section class="cta-section relative min-h-screen flex flex-col justify-center items-center">
  <!-- Background -->
  <div class="absolute inset-0 bg-gradient-to-br from-gray-900 via-gray-800 to-black">
    <!-- Optional background pattern -->
  </div>
  
  <!-- Content -->
  <div class="relative z-10 text-center text-white px-4 py-12">
    <h2 class="text-4xl md:text-6xl font-bold mb-6">
      <span class="text-red-500">Tu próxima</span>
      <br>
      <span>aventura empieza aquí</span>
    </h2>
    
    <p class="text-xl md:text-2xl mb-12 text-gray-300 max-w-2xl mx-auto">
      Descubre la familia Apache y encuentra el modelo perfecto para ti
    </p>
    
    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
      <button class="bg-red-500 hover:bg-red-600 text-white px-12 py-4 rounded-xl font-bold text-xl transition-all duration-300 transform hover:scale-105 hover:shadow-2xl">
        Solicitar Cotización
      </button>
      <button class="border-2 border-white/40 hover:border-red-500 hover:text-red-500 text-white px-12 py-4 rounded-xl font-bold text-xl transition-all duration-300 backdrop-blur-md">
        Encontrar Distribuidor
      </button>
    </div>
  </div>
  
  <!-- Bottom spacing for navbar -->
  <div class="h-24"></div>
</section>

<style>
  .cta-section {
    min-height: 100vh;
  }
  
  .cta-section button:hover {
    box-shadow: 0 10px 40px rgba(239, 68, 68, 0.4);
  }
  
  @media (max-width: 768px) {
    .cta-section h2 {
      font-size: 2.5rem;
    }
    
    .cta-section p {
      font-size: 1.125rem;
    }
    
    .cta-section button {
      font-size: 1rem;
      padding: 1rem 2rem;
    }
  }
</style>